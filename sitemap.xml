---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  
  <!-- Homepage -->
  <url>
    <loc>{{ site.url }}/</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>

  <!-- Alle pagina's behalve 404 en uitgesloten pagina's -->
  {% for page in site.pages %}
    {% if page.url != "/404.html" and page.sitemap != false and page.url != "/" %}
    <url>
      <loc>{{ site.url }}{{ page.url | replace:'index.html','' }}</loc>
      <lastmod>{{ page.date | default: site.time | date_to_xmlschema }}</lastmod>
      <changefreq>{% if page.changefreq %}{{ page.changefreq }}{% else %}monthly{% endif %}</changefreq>
      <priority>{% if page.priority %}{{ page.priority }}{% else %}0.8{% endif %}</priority>
    </url>
    {% endif %}
  {% endfor %}

  <!-- Blog posts/artikelen (als je die hebt) -->
  {% for post in site.posts %}
    {% unless post.sitemap == false %}
    <url>
      <loc>{{ site.url }}{{ post.url }}</loc>
      <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
      <changefreq>yearly</changefreq>
      <priority>0.7</priority>
    </url>
    {% endunless %}
  {% endfor %}

</urlset>
